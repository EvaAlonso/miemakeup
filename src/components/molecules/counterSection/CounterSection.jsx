/* import Proptypes from "prop-types";

export const CounterSection = ({value}) => {
  return (
    <>
      <h3>Añadir al carrito</h3>
      <h4> { value }</h4>

      <button onClick={}>
        +1
      </button>
    </>
  );
};

CounterSection.prototype = {
  value: Proptypes.number.isRequired
} */